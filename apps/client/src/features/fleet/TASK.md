# Task - implement ./components/CreateCarForm.tsx form sections

Create the form sections for the CreateCarForm based on the shared schema and types.

## constraints

- use existing shared schema and types for form validation and data structure.
- use existing components for form fields (e.g., TextInput, Select, Checkbox).
- install additional components if necessary from shadcn
- assemble the form sections in a logical order, grouping related fields together.
- do not handle form submission yet
- use radio group cards with icons for section selection (e.g., car type, fuel type).
